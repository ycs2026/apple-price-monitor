name: Apple Price Monitor

on:

  workflow_dispatch:

  schedule:

    - cron: "*/30 * * * *"

jobs:

  monitor:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout

        uses: actions/checkout@v4

      - name: Setup Node

        uses: actions/setup-node@v4

        with:

          node-version: 20

      - name: Install

        run: npm install

      - name: Run Monitor

        run: node src/index.js
